function acp() {
  git add .
  git commit -m "$1"
  git push
}

function acpu() {
  git add .
  git commit -m "$1"
  git push -u origin HEAD
}

# Create a new directory and enter it
function mkcd() {
    mkdir -p "$@" && cd "$@"
}

# Extract any archive
function extract() {
    if [ -f $1 ] ; then
        case $1 in
            *.tar.bz2)   tar xjf $1     ;;
            *.tar.gz)    tar xzf $1     ;;
            *.bz2)       bunzip2 $1     ;;
            *.rar)       unrar e $1     ;;
            *.gz)        gunzip $1      ;;
            *.tar)       tar xf $1      ;;
            *.tbz2)      tar xjf $1     ;;
            *.tgz)       tar xzf $1     ;;
            *.zip)       unzip $1       ;;
            *.Z)         uncompress $1  ;;
            *.7z)        7z x $1        ;;
            *)          echo "'$1' cannot be extracted via extract()" ;;
        esac
    else
        echo "'$1' is not a valid file"
    fi
}

# Create a backup of a file
function bak() {
    cp "$1" "$1.bak"
}

# Create a backup of a file with timestamp
function bakts() {
    cp "$1" "$1.$(date +%Y%m%d_%H%M%S).bak"
}

# Get IP address
function ip() {
    curl -s https://api.ipify.org
}

# Get local IP address
function localip() {
    ipconfig getifaddr en0
}

# Get weather
function weather() {
    curl -s "wttr.in/$1"
}

# Create a new git repository and push it to GitHub
function gitinit() {
    git init
    git add .
    git commit -m "Initial commit"
    git branch -M main
    git remote add origin $1
    git push -u origin main
}

# Create a new directory and initialize a git repository
function gitdir() {
    mkdir $1
    cd $1
    git init
}

# Create a new Python virtual environment
function pyvenv() {
    python3 -m venv venv
    source venv/bin/activate
}

# Create a new Node.js project
function nodeinit() {
    npm init -y
    git init
    echo "node_modules/" > .gitignore
}

# Create a new React project
function reactinit() {
    npx create-react-app $1
    cd $1
    npm install
}

# Create a new Next.js project
function nextinit() {
    npx create-next-app@latest $1
    cd $1
    npm install
}

# Create a new Vue.js project
function vueinit() {
    npm init vue@latest $1
    cd $1
    npm install
}

# Create a new Express.js project
function expressinit() {
    mkdir $1
    cd $1
    npm init -y
    npm install express
    echo "const express = require('express');" > index.js
    echo "const app = express();" >> index.js
    echo "const port = process.env.PORT || 3000;" >> index.js
    echo "app.get('/', (req, res) => res.send('Hello World!'));" >> index.js
    echo "app.listen(port, () => console.log(\`Server running at http://localhost:\${port}\`));" >> index.js
}

# Create a new Docker project
function dockerinit() {
    mkdir $1
    cd $1
    echo "FROM node:latest" > Dockerfile
    echo "WORKDIR /app" >> Dockerfile
    echo "COPY package*.json ./" >> Dockerfile
    echo "RUN npm install" >> Dockerfile
    echo "COPY . ." >> Dockerfile
    echo "EXPOSE 3000" >> Dockerfile
    echo "CMD [\"npm\", \"start\"]" >> Dockerfile
    echo "node_modules/" > .gitignore
    echo "npm-debug.log" >> .gitignore
}

# Create a new Kubernetes deployment
function k8sinit() {
    mkdir $1
    cd $1
    kubectl create deployment $1 --image=nginx --dry-run=client -o yaml > deployment.yaml
    kubectl create service clusterip $1 --tcp=80:80 --dry-run=client -o yaml > service.yaml
}

# Create a new Terraform project
function tfinit() {
    mkdir $1
    cd $1
    terraform init
    echo "provider \"aws\" {" > main.tf
    echo "  region = \"us-east-1\"" >> main.tf
    echo "}" >> main.tf
}

# Create a new Ansible project
function ansibleinit() {
    mkdir $1
    cd $1
    mkdir -p roles/{common,webserver,database}
    touch inventory
    touch site.yml
    echo "all:" > inventory
    echo "  hosts:" >> inventory
    echo "    webservers:" >> inventory
    echo "      hosts:" >> inventory
    echo "        web1:" >> inventory
    echo "          ansible_host: 192.168.1.10" >> inventory
    echo "---" > site.yml
    echo "- name: Configure servers" >> site.yml
    echo "  hosts: all" >> site.yml
    echo "  become: yes" >> site.yml
    echo "  roles:" >> site.yml
    echo "    - common" >> site.yml
}

# Create a new Python package
function pypkginit() {
    mkdir $1
    cd $1
    mkdir $1
    touch $1/__init__.py
    touch setup.py
    touch README.md
    echo "# $1" > README.md
    echo "from setuptools import setup, find_packages" > setup.py
    echo "setup(" >> setup.py
    echo "    name=\"$1\"," >> setup.py
    echo "    version=\"0.1.0\"," >> setup.py
    echo "    packages=find_packages()," >> setup.py
    echo "    install_requires=[]," >> setup.py
    echo "    author=\"Your Name\"," >> setup.py
    echo "    author_email=\"your.email@example.com\"," >> setup.py
    echo "    description=\"A short description of your package\"," >> setup.py
    echo "    long_description=open('README.md').read()," >> setup.py
    echo "    long_description_content_type=\"text/markdown\"," >> setup.py
    echo "    url=\"https://github.com/yourusername/$1\"," >> setup.py
    echo "    classifiers=[" >> setup.py
    echo "        \"Programming Language :: Python :: 3\"," >> setup.py
    echo "        \"License :: OSI Approved :: MIT License\"," >> setup.py
    echo "        \"Operating System :: OS Independent\"," >> setup.py
    echo "    ]," >> setup.py
    echo "    python_requires='>=3.6'," >> setup.py
    echo ")" >> setup.py
}

# Enhanced ls with file preview (no hidden files)
function lsp() {
    # Get all items (files and directories) excluding hidden files
    local items=$(ls -1 2>/dev/null | sort)
    
    # If no items found, show message
    if [[ -z "$items" ]]; then
        echo "Empty directory"
        return
    fi
    
    # Use fzf to browse with enhanced preview
    echo "$items" | fzf \
        --height=80% \
        --layout=reverse \
        --border \
        --preview-window=right:60% \
        --preview='
            if [[ -d {} ]]; then
                echo "📁 Directory: {}"
                echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                
                # Check if directory is readable
                if [[ -r {} ]]; then
                    size=$(du -sh {} 2>/dev/null | cut -f1)
                    if [[ -n "$size" ]]; then
                        echo "📊 Size: $size"
                    else
                        echo "📊 Size: calculating..."
                    fi
                    
                    count=$(find {} -maxdepth 1 2>/dev/null | wc -l | tr -d " ")
                    if [[ $count -gt 1 ]]; then
                        echo "📈 Items: $((count - 1)) files/folders"
                    else
                        echo "📈 Items: 0 files/folders"
                    fi
                    
                    echo ""
                    echo "📄 Contents:"
                    
                    # Try different listing approaches
                    if ls -la {} >/dev/null 2>&1; then
                        ls -la {} 2>/dev/null | head -15
                        remaining=$(ls -1 {} 2>/dev/null | wc -l | tr -d " ")
                        if [[ $remaining -gt 15 ]]; then
                            echo "..."
                            echo "... and $((remaining - 15)) more items"
                        fi
                    elif find {} -maxdepth 1 >/dev/null 2>&1; then
                        echo "Using find (ls failed):"
                        find {} -maxdepth 1 -not -path {} 2>/dev/null | head -15 | sed "s|^{}/||"
                    else
                        echo "❌ Cannot access directory contents"
                    fi
                else
                    echo "❌ Permission denied - cannot read directory"
                    echo "📊 Size: unknown"
                    echo "📈 Items: unknown"
                fi
            else
                echo "📄 File: {}"
                echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                
                # File size and permissions
                if [[ -r {} ]]; then
                    size=$(ls -lh {} 2>/dev/null | awk "{print \$5}")
                    echo "📊 Size: ${size:-unknown}"
                    filetype=$(file -b {} 2>/dev/null)
                    echo "🔍 Type: ${filetype:-unknown}"
                    echo ""
                    echo "📄 Preview:"
                    
                    # Enhanced file type detection and preview
                    if [[ -f {} ]]; then
                        case "${{}##*.}" in
                            txt|md|markdown|readme|README)
                                head -20 {} 2>/dev/null || echo "Cannot read file"
                                ;;
                            json|js|ts|jsx|tsx|py|sh|zsh|bash|yml|yaml|xml|html|css|scss|sass)
                                head -20 {} 2>/dev/null || echo "Cannot read file"
                                ;;
                            jpg|jpeg|png|gif|bmp|svg|ico)
                                echo "🖼️  Image file - cannot preview in terminal"
                                echo "📐 Try: open {} (to view)"
                                ;;
                            pdf)
                                echo "📋 PDF file - cannot preview in terminal"
                                echo "📖 Try: open {} (to view)"
                                ;;
                            zip|tar|gz|7z|rar)
                                echo "🗜️  Archive file"
                                if command -v unzip >/dev/null && [[ {} == *.zip ]]; then
                                    echo "Contents:"
                                    unzip -l {} 2>/dev/null | head -10
                                elif command -v tar >/dev/null && [[ {} == *.tar* ]]; then
                                    echo "Contents:"
                                    tar -tf {} 2>/dev/null | head -10
                                fi
                                ;;
                            *)
                                if [[ $(file -b {} 2>/dev/null) == *"text"* ]]; then
                                    head -20 {} 2>/dev/null || echo "Cannot read file"
                                elif [[ $(file -b {} 2>/dev/null) == *"binary"* ]]; then
                                    echo "🔢 Binary file - cannot preview"
                                    echo "🔍 File type: $(file -b {} 2>/dev/null)"
                                else
                                    head -10 {} 2>/dev/null || echo "Cannot determine file type"
                                fi
                                ;;
                        esac
                    else
                        echo "❓ Unknown file type"
                    fi
                else
                    echo "❌ Permission denied - cannot read file"
                fi
            fi
        ' \
        --header='📁 Browse files and directories (ENTER to select, ESC to cancel)' \
        --prompt='Browse> ' \
        --bind='enter:execute(
            if [[ -d {} ]]; then
                echo "📁 Entering directory: {}"
                cd {} && pwd && echo "" && ls -la
            else
                echo "📄 Opening file: {}"
                if command -v open >/dev/null; then
                    open {}
                elif command -v xdg-open >/dev/null; then
                    xdg-open {}
                else
                    echo "❌ Cannot open file - no suitable command found"
                fi
            fi
        )'
}

# Enhanced ls with file preview (including hidden files)
function lsph() {
    # Get all items (files and directories) including hidden files
    local items=$(ls -1A 2>/dev/null | sort)
    
    # If no items found, show message
    if [[ -z "$items" ]]; then
        echo "Empty directory"
        return
    fi
    
    # Use fzf to browse with enhanced preview
    echo "$items" | fzf \
        --height=80% \
        --layout=reverse \
        --border \
        --preview-window=right:60% \
        --preview='
            if [[ -d {} ]]; then
                echo "📁 Directory: {}"
                echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                
                # Check if directory is readable
                if [[ -r {} ]]; then
                    size=$(du -sh {} 2>/dev/null | cut -f1)
                    if [[ -n "$size" ]]; then
                        echo "📊 Size: $size"
                    else
                        echo "📊 Size: calculating..."
                    fi
                    
                    count=$(find {} -maxdepth 1 2>/dev/null | wc -l | tr -d " ")
                    if [[ $count -gt 1 ]]; then
                        echo "📈 Items: $((count - 1)) files/folders"
                    else
                        echo "📈 Items: 0 files/folders"
                    fi
                    
                    echo ""
                    echo "📄 Contents (including hidden):"
                    
                    # Try different listing approaches with hidden files
                    if ls -laA {} >/dev/null 2>&1; then
                        ls -laA {} 2>/dev/null | head -15
                        remaining=$(ls -1A {} 2>/dev/null | wc -l | tr -d " ")
                        if [[ $remaining -gt 15 ]]; then
                            echo "..."
                            echo "... and $((remaining - 15)) more items"
                        fi
                    elif find {} -maxdepth 1 >/dev/null 2>&1; then
                        echo "Using find (ls failed):"
                        find {} -maxdepth 1 -not -path {} 2>/dev/null | head -15 | sed "s|^{}/||"
                    else
                        echo "❌ Cannot access directory contents"
                    fi
                else
                    echo "❌ Permission denied - cannot read directory"
                    echo "📊 Size: unknown"
                    echo "📈 Items: unknown"
                fi
            else
                echo "📄 File: {}"
                echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
                
                # File size and permissions
                if [[ -r {} ]]; then
                    size=$(ls -lh {} 2>/dev/null | awk "{print \$5}")
                    echo "📊 Size: ${size:-unknown}"
                    filetype=$(file -b {} 2>/dev/null)
                    echo "🔍 Type: ${filetype:-unknown}"
                    echo ""
                    echo "📄 Preview:"
                    
                    # Enhanced file type detection and preview
                    if [[ -f {} ]]; then
                        case "${{}##*.}" in
                            txt|md|markdown|readme|README)
                                head -20 {} 2>/dev/null || echo "Cannot read file"
                                ;;
                            json|js|ts|jsx|tsx|py|sh|zsh|bash|yml|yaml|xml|html|css|scss|sass)
                                head -20 {} 2>/dev/null || echo "Cannot read file"
                                ;;
                            jpg|jpeg|png|gif|bmp|svg|ico)
                                echo "🖼️  Image file - cannot preview in terminal"
                                echo "📐 Try: open {} (to view)"
                                ;;
                            pdf)
                                echo "📋 PDF file - cannot preview in terminal"
                                echo "📖 Try: open {} (to view)"
                                ;;
                            zip|tar|gz|7z|rar)
                                echo "🗜️  Archive file"
                                if command -v unzip >/dev/null && [[ {} == *.zip ]]; then
                                    echo "Contents:"
                                    unzip -l {} 2>/dev/null | head -10
                                elif command -v tar >/dev/null && [[ {} == *.tar* ]]; then
                                    echo "Contents:"
                                    tar -tf {} 2>/dev/null | head -10
                                fi
                                ;;
                            *)
                                if [[ $(file -b {} 2>/dev/null) == *"text"* ]]; then
                                    head -20 {} 2>/dev/null || echo "Cannot read file"
                                elif [[ $(file -b {} 2>/dev/null) == *"binary"* ]]; then
                                    echo "🔢 Binary file - cannot preview"
                                    echo "🔍 File type: $(file -b {} 2>/dev/null)"
                                else
                                    head -10 {} 2>/dev/null || echo "Cannot determine file type"
                                fi
                                ;;
                        esac
                    else
                        echo "❓ Unknown file type"
                    fi
                else
                    echo "❌ Permission denied - cannot read file"
                fi
            fi
        ' \
        --header='📁 Browse files and directories with hidden files (ENTER to select, ESC to cancel)' \
        --prompt='Browse+Hidden> ' \
        --bind='enter:execute(
            if [[ -d {} ]]; then
                echo "📁 Entering directory: {}"
                cd {} && pwd && echo "" && ls -laA
            else
                echo "📄 Opening file: {}"
                if command -v open >/dev/null; then
                    open {}
                elif command -v xdg-open >/dev/null; then
                    xdg-open {}
                else
                    echo "❌ Cannot open file - no suitable command found"
                fi
            fi
        )'
}

# Brain sync - pull latest notes before opening claude
function brain-sync() {
    local notes_dir="$HOME/.notes"
    if [[ -d "$notes_dir/.git" ]]; then
        git -C "$notes_dir" fetch origin 2>/dev/null
        local behind=$(git -C "$notes_dir" rev-list HEAD..origin/main --count 2>/dev/null)
        if [[ "$behind" -gt 0 ]]; then
            echo "[brain] $behind commits behind remote, pulling..."
            git -C "$notes_dir" pull --rebase origin main 2>/dev/null && echo "[brain] synced" || echo "[brain] pull failed, continuing..."
        else
            echo "[brain] up to date"
        fi
        local ahead=$(git -C "$notes_dir" rev-list origin/main..HEAD --count 2>/dev/null)
        if [[ "$ahead" -gt 0 ]]; then
            echo "[brain] $ahead local commits not pushed"
        fi
    fi
}

# Brain push - commit and push notes
function brain-push() {
    local notes_dir="$HOME/.notes"
    local msg="${1:-brain: update}"
    if [[ -d "$notes_dir/.git" ]]; then
        cd "$notes_dir"
        if [[ -n $(git status --porcelain) ]]; then
            git add -A && git commit -m "$msg" && git push origin main && echo "[brain] pushed" || echo "[brain] push failed"
        else
            local ahead=$(git rev-list origin/main..HEAD --count 2>/dev/null)
            if [[ "$ahead" -gt 0 ]]; then
                git push origin main && echo "[brain] $ahead commits pushed" || echo "[brain] push failed"
            else
                echo "[brain] nothing to push"
            fi
        fi
        cd - > /dev/null
    fi
}

# Sync brain - add, commit, and push notes
function sync-brain {
    local commitMessage=$1
    if [ -z "$commitMessage" ]; then
        commitMessage="brain: updates"
    fi
    cd ~/.notes && git add -A && git commit -m "$commitMessage" && git push
    echo "[brain] synced"
}

# Sync workspace repos: clone missing, fast-forward existing.
# Manifest: ~/.dotfiles/configs/repos.txt (format: bucket/repo<TAB>url)
# Usage:
#   wsync                  # sync all
#   wsync --dry-run        # show what would happen
#   wsync --only isaac     # filter by bucket (isaac|personal|...)
#   wsync --scan           # rebuild manifest from current ~/www state
function wsync() {
  local base="$HOME/www"
  local manifest="$HOME/.dotfiles/configs/repos.txt"
  local dry_run=0 only="" scan=0

  while [ $# -gt 0 ]; do
    case "$1" in
      --dry-run) dry_run=1 ;;
      --only) only="$2"; shift ;;
      --scan) scan=1 ;;
      -h|--help)
        echo "wsync [--dry-run] [--only <bucket>] [--scan]"
        return 0
        ;;
      *) echo "wsync: unknown arg: $1" >&2; return 2 ;;
    esac
    shift
  done

  if [ "$scan" -eq 1 ]; then
    mkdir -p "$(dirname "$manifest")"
    : > "$manifest.tmp"
    for bucket_dir in "$base"/*/; do
      [ -d "$bucket_dir" ] || continue
      local bucket=$(basename "$bucket_dir")
      for repo_dir in "$bucket_dir"*/; do
        [ -d "$repo_dir/.git" ] || continue
        local repo=$(basename "$repo_dir")
        local url=$(git -C "$repo_dir" remote get-url origin 2>/dev/null) || continue
        printf "%s/%s\t%s\n" "$bucket" "$repo" "$url" >> "$manifest.tmp"
      done
    done
    sort "$manifest.tmp" > "$manifest" && rm "$manifest.tmp"
    echo "[wsync] manifest updated: $(wc -l < "$manifest" | tr -d ' ') repos -> $manifest"
    return 0
  fi

  if [ ! -f "$manifest" ]; then
    echo "[wsync] manifest not found at $manifest" >&2
    echo "        run: wsync --scan  (on a machine with repos already cloned)" >&2
    return 1
  fi

  local total=0 cloned=0 pulled=0 skipped=0 failed=0
  while IFS=$'\t' read -r slug url; do
    [ -z "$slug" ] && continue
    [ "${slug#\#}" != "$slug" ] && continue
    local bucket="${slug%%/*}"
    local repo="${slug#*/}"
    [ -n "$only" ] && [ "$bucket" != "$only" ] && continue
    total=$((total + 1))
    local target="$base/$bucket/$repo"

    if [ ! -d "$target/.git" ]; then
      echo "[wsync] clone  $slug"
      if [ "$dry_run" -eq 1 ]; then
        cloned=$((cloned + 1))
      else
        mkdir -p "$base/$bucket"
        if git clone "$url" "$target"; then
          cloned=$((cloned + 1))
        else
          failed=$((failed + 1))
        fi
      fi
      continue
    fi

    local branch=$(git -C "$target" symbolic-ref --short HEAD 2>/dev/null)
    local default=$(git -C "$target" symbolic-ref --short refs/remotes/origin/HEAD 2>/dev/null | sed 's|^origin/||')
    [ -z "$default" ] && default="main"

    if [ "$branch" != "$default" ]; then
      echo "[wsync] skip   $slug (on $branch, default=$default)"
      skipped=$((skipped + 1))
      continue
    fi

    local dirty=0
    [ -n "$(git -C "$target" status --porcelain 2>/dev/null)" ] && dirty=1

    echo "[wsync] pull   $slug ($branch)"
    if [ "$dry_run" -eq 1 ]; then
      pulled=$((pulled + 1))
      continue
    fi

    if [ "$dirty" -eq 1 ]; then
      if ! git -C "$target" stash push -u -m "wsync auto-stash $(date +%Y%m%d-%H%M%S)" >/dev/null; then
        echo "        ! stash failed, skipping"
        failed=$((failed + 1))
        continue
      fi
    fi

    if git -C "$target" fetch --quiet && git -C "$target" pull --ff-only --quiet; then
      pulled=$((pulled + 1))
    else
      echo "        ! pull failed (non-ff or network)"
      failed=$((failed + 1))
    fi

    if [ "$dirty" -eq 1 ]; then
      git -C "$target" stash pop >/dev/null 2>&1 || echo "        ! stash pop conflicted, check 'git stash list' in $target"
    fi
  done < "$manifest"

  echo "[wsync] done: $total total, $cloned cloned, $pulled pulled, $skipped skipped, $failed failed"
}

# Load local/org-specific functions if available
[ -f "$HOME/.zsh_functions_local" ] && source "$HOME/.zsh_functions_local"