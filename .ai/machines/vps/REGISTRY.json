{
  "_doc": "VPS machine — source of truth for Claude Atlas scope registry on the headless service node (hq.gripp.link). Edited by hand. Read by ~/.claude/bin/atlas-sync. Paths use ~ (expanded to $HOME at runtime on the physical machine; VPS: /home/grippado). Service node only — no dev scopes.",
  "version": 2,
  "scopes": {
    "notes": {
      "path": "~/.notes",
      "claude_dir": ".claude",
      "shared": false
    }
  }
}
